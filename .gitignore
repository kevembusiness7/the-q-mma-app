node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
*.log
.DS_Store

# Arquivos temporarios do Supabase CLI
supabase/.temp/

# Artes originais em PNG (~8MB) das quais os .webp servidos foram gerados.
# Ficam fora de public/ pra nao irem pro build, e fora do git pra nao pesarem
# o historico pra sempre. Guardar junto do resto do material de design.
originais/
